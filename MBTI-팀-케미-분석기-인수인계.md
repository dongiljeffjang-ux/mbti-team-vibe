# MBTI 팀 케미 분석기

## 1. 문서 목적

현재까지 구현된 서비스의 기능, 기술 구조, AI 연동 방식, 사주 참고 분석, 배포 방법과 운영 시 주의사항을 동료 개발자에게 전달하기 위한 인수인계 문서입니다.

서비스 주소: [https://mbti-team-vibe.vercel.app](https://mbti-team-vibe.vercel.app)

GitHub 저장소: [dongiljeffjang-ux/mbti-team-vibe](https://github.com/dongiljeffjang-ux/mbti-team-vibe)

## 2. 서비스 한 줄 설명

팀 목표와 상황, 팀원별 MBTI·역할·업무 스타일을 입력하면 규칙 기반 팀 케미 지표와 OpenAI 기반 협업 해설을 함께 제공하는 웹 서비스입니다.

사주 정보는 선택 입력으로 제공하며, 생년월일과 생시가 모두 입력된 팀원에 한해 만세력 결과를 협업 관점의 참고 정보로 해석합니다.

## 3. 현재 사용자 흐름

```text
팀 이름 입력
  → 팀 목표 직접 입력
  → 팀 목적·현재 상황 20~1200자 입력
  → 팀원별 이름·역할·MBTI·업무 스타일 입력
  → 선택적으로 생년월일·생시·양력/음력·성별 입력
  → 팀 케미 분석하기
  → 규칙 기반 수치 결과 즉시 표시
  → OpenAI 해설 생성
  → 강점·개인 기여·갈등·시너지·사주 참고 해설 확인
  → 공유 링크 또는 PDF 저장
```

## 4. 입력 항목

### 팀 정보

| 항목 | 필수 | 제한/설명 |
|---|---:|---|
| 팀 이름 | 예 | 최대 80자 |
| 팀 목표 | 예 | 직접 입력, 최대 120자 |
| 팀 목적과 상황 | 예 | 20자 이상 1200자 이하 |

팀 목표는 더 이상 선택형 칩이 아닙니다. 사용자가 직접 입력한 문장이 AI 분석에 전달됩니다. 목표에는 달성 결과, 기한, 성공 기준을 구체적으로 적는 것이 좋습니다.

### 팀원 정보

| 항목 | 필수 | 제한/설명 |
|---|---:|---|
| 이름 | 아니오 | 비어 있으면 `팀원 N`으로 표시 |
| 역할/직무 | 아니오 | 비어 있으면 `미정` |
| MBTI | 예 | 팀원 2명 이상, 최대 10명 |
| 업무 스타일·강점·고민 | 아니오 | 최대 300자 |
| 생년월일 | 선택 | 생시와 함께 입력해야 함 |
| 생시 | 선택 | `HH:mm` 형식 |
| 양력/음력 | 선택 | 기본값 양력 |
| 성별 | 선택 | 미입력/남성/여성 |

생년월일 선택 범위는 1950년 1월 1일부터 현재 날짜까지입니다. 미래 날짜는 선택할 수 없습니다.

## 5. 결과 구성

### 규칙 기반 결과

`lib/rules.js`가 담당합니다.

- 팀 코드
- 팀 케미 점수
- E/I, S/N, T/F, J/P 성향 비중
- 축별 배합 비율
- 최고 궁합·최저 궁합
- 규칙 기반 강점
- 규칙 기반 갈등 신호
- 보완이 필요한 MBTI 유형
- 팀원 추가 시뮬레이션
- 역할별 추천

규칙 기반 계산은 서버와 클라이언트 양쪽에서 수행합니다. 서버는 클라이언트가 보낸 점수나 비율을 신뢰하지 않고 `analyze()`를 다시 실행합니다.

### OpenAI 생성 결과

`api/narrative.js`가 담당합니다.

- 팀 목표를 고려한 전체 협업 전망
- 팀 핵심 강점
- 팀원별 역할·강점과 주의점
- 최고 궁합 조합의 활용법
- 실제 업무 맥락 기반 잠재 갈등
- 갈등이 가장 크게 발생할 수 있는 시나리오 1개
- 협업 시너지가 가장 크게 발생하는 시나리오 1개
- 팀 협업 규칙과 액션 플랜
- 사주 참고 요약
- 팀원별 일간·오행·협업 참고 해설

갈등과 시나리오는 규칙 기반 제목을 단순 출력하지 않고, 팀 목표·목적·직무·업무 스타일을 함께 전달해 AI가 업무 장면으로 재구성하도록 되어 있습니다.

## 6. AI API 구조

### 엔드포인트

```text
POST /api/narrative
```

### 요청 예시

```json
{
  "teamName": "신규 서비스 TF",
  "goal": "3개월 안에 고객용 모바일 서비스를 출시하고 초기 사용자를 확보한다.",
  "purpose": "기획·개발·디자인·고객 커뮤니케이션이 빠르게 협업해야 합니다. 현재는 의사결정이 늦고 일정 변경에 대한 불만이 있습니다.",
  "members": [
    {
      "name": "김지훈",
      "type": "ENFP",
      "role": "기획 리드",
      "note": "새로운 아이디어를 빠르게 제안하는 편",
      "birthDate": "1995-04-12",
      "birthTime": "08:30",
      "birthCalendar": "solar",
      "gender": "male"
    }
  ]
}
```

### 처리 순서

1. HTTP 메서드 확인
2. 팀 이름·목표·목적·팀원 수 검증
3. MBTI 유형 검증
4. 생년월일과 생시가 함께 입력됐는지 검증
5. 서버에서 MBTI 수치 재계산
6. 입력된 사주 정보가 있으면 `manseryeok`으로 사주 계산
7. MBTI·사주·팀 목표·역할·업무 스타일을 JSON 컨텍스트로 구성
8. Supabase 캐시 조회
9. 캐시가 없으면 OpenAI 호출
10. JSON 응답 형식 검증
11. 결과 캐싱 후 클라이언트에 반환

### OpenAI 호출 방식

현재 Chat Completions API를 사용합니다.

```text
POST https://api.openai.com/v1/chat/completions
model: OPENAI_MODEL 또는 gpt-4o-mini
response_format: { type: "json_object" }
```

API 키는 브라우저에 노출하지 않습니다. `/api/narrative.js` 서버리스 함수에서만 `OPENAI_API_KEY`를 읽습니다.

## 7. AI 프롬프트 원칙

프롬프트에는 다음 원칙이 포함되어 있습니다.

- 한국어 해요체 사용
- 문장을 짧고 실무적으로 작성
- MBTI를 성격이나 능력의 확정적 진단으로 표현하지 않기
- 팀 목표와 현재 상황을 반드시 고려하기
- 역할과 업무 스타일을 MBTI와 함께 해석하기
- 규칙 기반 숫자를 다시 계산하거나 변경하지 않기
- 갈등은 가능성으로 표현하기
- 해결책은 실제 회의·업무 행동으로 제시하기
- 사주는 명리학적 참고 해석으로만 표현하기
- 사주가 성과·성격·운명을 결정한다고 말하지 않기
- 오행 분포의 근거를 명시하기

사주 해설 예시는 다음과 같은 방향입니다.

```text
표면 오행 분포상 화(火)가 상대적으로 강한 편으로 나타나며,
이를 명리학적 참고 관점에서는 표현과 추진 에너지가 빠르게 드러나는 경향으로 해석할 수 있습니다.
다만 이는 출생 정보에서 계산된 참고 지표이며 개인의 성격이나 업무 성과를 결정하지 않습니다.
```

## 8. 사주 참고 분석

### 사용 라이브러리

```text
manseryeok ^2.0.0
```

`calculateFourPillars()`로 년주·월주·일주·시주를 계산합니다. 양력/음력, 생년월일, 생시, 성별을 입력으로 사용합니다.

### 서버 내부 처리

`api/narrative.js`의 `buildSajuProfile()`이 담당합니다.

계산 결과로 다음 데이터를 구성합니다.

- 사주 네 기둥
- 한자 표기
- 일간 오행
- 일간 음양
- 표면 오행 분포
- 상대적으로 많은 오행
- 십신

오행 분포는 천간과 지지의 표면 글자를 기준으로 집계합니다. 따라서 결과 화면에서는 이를 “표면 오행 분포”라고 표현하며, 절대적인 기질이나 운세로 단정하지 않습니다.

### 입력 조건

생년월일만 입력하거나 생시만 입력하면 서버에서 400 오류를 반환합니다. 정확한 사주 참고 분석을 위해 두 항목을 모두 입력해야 합니다.

사주 정보가 없는 팀원은 MBTI 분석에만 포함됩니다.

## 9. 개인정보 및 저장 정책

### OpenAI 전송

사용자가 입력한 팀 이름, 목표, 목적, 팀원 이름, 역할, 업무 스타일, MBTI, 사주 참고 정보가 서버 프롬프트 컨텍스트에 포함될 수 있습니다.

따라서 실제 서비스 운영 시 입력 안내와 개인정보 처리방침을 별도로 제공하는 것이 좋습니다.

### 공유 링크

공유 링크 생성 시 현재 저장되는 팀원 데이터는 다음 항목으로 제한되어 있습니다.

```json
{
  "name": "이름",
  "type": "MBTI",
  "role": "역할",
  "note": "업무 스타일"
}
```

생년월일, 생시, 성별, 양력/음력 정보는 공유 링크 저장 데이터에 포함하지 않습니다.

### Supabase

Supabase가 설정된 경우 `narrative_cache` 테이블에 AI 결과를 14일 동안 캐시합니다. 캐시 키에는 입력 내용과 사주 입력값이 포함되지만, 캐시 payload에는 생성 결과가 저장됩니다.

운영 환경에서는 Supabase 접근 정책, 보관 기간, 삭제 정책을 확인해야 합니다.

## 10. 캐시 동작

캐시 키는 다음 정보를 SHA-256으로 해시합니다.

- 팀 이름
- 팀 목표
- 팀 목적
- 팀원 이름
- MBTI
- 역할
- 업무 스타일
- 생년월일
- 생시
- 양력/음력
- 성별
- 모델명
- 캐시 버전

사주 입력을 추가하면서 캐시 버전은 `v: 6`으로 올렸습니다. 프롬프트 구조를 바꾸는 경우에도 캐시 버전을 올리는 것이 안전합니다.

## 11. 주요 파일 구조

```text
mbti 팀/
├─ api/
│  └─ narrative.js          # OpenAI 서버리스 API, 검증, 캐시, 사주 계산
├─ lib/
│  └─ rules.js              # MBTI 규칙 기반 분석 엔진
├─ src/
│  ├─ App.jsx               # 전체 입력·결과 화면과 상태 관리
│  ├─ styles.css            # 기본 스타일, 보고서 레이아웃, 인쇄 스타일
│  └─ lib/
│     └─ supabase.js        # 공유 링크 생성·조회
├─ supabase/
│  └─ schema.sql            # 공유 및 AI 캐시 테이블 스키마
├─ package.json
├─ vercel.json
├─ vite.config.js
└─ index.html
```

## 12. 환경변수

Vercel 프로젝트 환경변수에 다음을 설정합니다.

```env
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
SUPABASE_URL=https://프로젝트.supabase.co
SUPABASE_SERVICE_ROLE_KEY=...
VITE_SUPABASE_URL=https://프로젝트.supabase.co
VITE_SUPABASE_ANON_KEY=...
```

필수값은 `OPENAI_API_KEY`입니다. `OPENAI_MODEL`은 선택값이며 기본값은 `gpt-4o-mini`입니다.

환경변수 변경 후에는 반드시 Vercel에서 Redeploy해야 합니다.

## 13. 로컬 실행

```powershell
npm install
npm run dev
```

프론트엔드만 확인할 때는 Vite 개발 서버를 사용합니다.

Vercel 서버리스 API까지 함께 확인하려면 Vercel CLI 환경에서 실행합니다.

```powershell
npx vercel dev
```

프로덕션 빌드 검증:

```powershell
npm run build
```

## 14. 배포

현재 배포는 GitHub 저장소와 Vercel 프로젝트를 연결한 방식입니다.

Vercel CLI로 직접 배포하는 경우:

```powershell
npx vercel --prod --yes --name mbti-team-vibe
```

배포 성공 시 다음 주소가 프로덕션 별칭으로 연결됩니다.

```text
https://mbti-team-vibe.vercel.app
```

GitHub Pages workflow는 현재 주 배포 경로가 아닙니다. 과거 `actions/deploy-pages` 오류가 있었기 때문에, 운영 확인은 Vercel 배포 상태를 기준으로 합니다.

## 15. PDF 내보내기

상단 `PDF 내보내기` 버튼은 `window.print()`를 호출합니다.

인쇄 전용 CSS에서 다음 요소를 숨깁니다.

- 좌측 사이드바
- 상단 내비게이션
- 입력 영역
- 분석 버튼
- 공유 UI
- 시뮬레이터
- 삭제 버튼

브라우저 인쇄 창에서 대상 프린터를 `PDF로 저장`으로 선택하면 보고서 PDF를 저장할 수 있습니다.

## 16. 현재 디자인

Stitch에서 전달받은 보고서형 디자인을 기준으로 다음 방향을 적용했습니다.

- 좌측 보고서 내비게이션
- 네이비·화이트·라벤더 기반 색상
- 보고서 번호와 섹션 구조
- 팀 요약 및 결과 카드
- AI 인사이트와 시나리오 카드
- 사주 참고 분석 카드
- 인쇄 가능한 리포트 레이아웃
- 모바일에서 축소되는 사이드바와 입력행

## 17. 오류 처리

AI 해설 요청에 실패해도 규칙 기반 계산 결과는 유지됩니다.

주요 오류 메시지:

```text
OPENAI_API_KEY 환경변수가 없습니다.
해설 생성에 실패했습니다.
강점 응답 형식이 올바르지 않습니다.
갈등 응답 형식이 올바르지 않습니다.
사주 분석에는 생년월일과 생시를 모두 입력해 주세요.
```

AI 응답은 JSON 형식으로 요청하고 서버에서 필수 필드를 검증합니다. 형식이 잘못되면 클라이언트는 수치 계산 결과를 그대로 보여주고 AI 해설 오류를 안내합니다.

## 18. 테스트 체크리스트

### 일반 분석

- [ ] 팀 이름을 입력한다.
- [ ] 팀 목표를 직접 입력한다.
- [ ] 목적과 상황을 20자 이상 입력한다.
- [ ] 팀원 2명 이상에 MBTI를 지정한다.
- [ ] 분석 버튼이 활성화되는지 확인한다.
- [ ] 팀 케미 점수가 표시되는지 확인한다.
- [ ] AI 강점과 갈등 해설이 표시되는지 확인한다.

### 사주 분석

- [ ] 생년월일과 생시를 모두 입력한다.
- [ ] 양력/음력 값을 확인한다.
- [ ] 사주 참고 카드가 표시되는지 확인한다.
- [ ] 일간·오행 근거가 포함되는지 확인한다.
- [ ] 생년월일만 입력했을 때 안내 오류가 표시되는지 확인한다.
- [ ] 사주 정보 없이 분석하면 사주 카드가 불필요하게 나타나지 않는지 확인한다.

### 공유·출력

- [ ] 공유 링크가 생성되는지 확인한다.
- [ ] 공유 링크에 생년월일·생시가 노출되지 않는지 확인한다.
- [ ] PDF 내보내기 버튼이 인쇄 창을 여는지 확인한다.
- [ ] PDF에서 사이드바와 입력 버튼이 제외되는지 확인한다.

## 19. 운영상 주의사항

1. OpenAI API 키는 절대 `VITE_` 접두사를 붙여 프론트엔드에 노출하지 않습니다.
2. Vercel 환경변수 입력 후 반드시 Redeploy합니다.
3. AI 응답 형식을 바꾸면 서버 검증과 클라이언트 검증을 함께 수정합니다.
4. 프롬프트 형식을 바꾸면 캐시 버전을 올립니다.
5. 사주 분석 결과는 문화적 참고 정보로만 표시합니다.
6. MBTI와 사주를 채용·평가·인사 판단에 사용하지 않도록 안내합니다.
7. 실제 회사 구성원의 생년월일을 입력받을 경우 개인정보 처리방침과 보관·삭제 정책을 마련합니다.
8. API 비용이 증가하면 캐시 기간, 호출 횟수 제한, 요청 길이 제한을 조정합니다.

## 20. 향후 개선 우선순위

### 우선순위 높음

- [ ] 개인정보 처리방침 및 사주 입력 동의 문구 추가
- [ ] AI API 호출 중복 클릭 방지 강화
- [ ] OpenAI 오류 코드별 사용자 안내 개선
- [ ] 사주 입력값의 날짜·시간 경계 검증 강화
- [ ] 팀 목표와 목적의 필수 입력 안내 개선

### 우선순위 중간

- [ ] PDF 전용 페이지 나눔과 표지 추가
- [ ] 보고서 내비게이션을 실제 섹션 앵커와 연결
- [ ] AI 결과 재생성 버튼 추가
- [ ] AI 모델 및 생성 시각 표시
- [ ] 공유 링크에서 저장된 분석의 사주 미포함 정책을 UI에 명시

### 우선순위 낮음

- [ ] 팀별 분석 이력 저장
- [ ] 분석 전후 팀 변화 비교
- [ ] 실제 협업 피드백 수집
- [ ] MBTI 외 업무 선호도 설문 결합
- [ ] 조직 관리자용 여러 팀 대시보드

## 21. 동료에게 전달할 핵심 요약

이 프로젝트는 `rules.js`가 수치와 기본 분석을 담당하고, `api/narrative.js`가 OpenAI를 통해 팀 목표와 업무 맥락을 반영한 문장형 해설을 생성하는 구조입니다.

사주 기능은 `manseryeok`으로 네 기둥을 계산한 뒤 일간·오행·십신을 AI 컨텍스트에 포함합니다. 단, 사주 정보는 선택 입력이며 생년월일과 생시가 모두 있어야 계산됩니다.

프론트엔드는 `src/App.jsx`, 스타일은 `src/styles.css`에 집중되어 있습니다. API 키는 Vercel 환경변수에서 관리하며, 소스 코드나 클라이언트 번들에 넣으면 안 됩니다.

현재 프로덕션 주소는 다음과 같습니다.

```text
https://mbti-team-vibe.vercel.app
```

